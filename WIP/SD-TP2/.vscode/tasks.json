{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile C",
            "type": "shell",
            "command": "gcc -g Client/*.c -o Client/client\ngcc -g Server/*.c -o Server/server",
            // "command": "gcc -g void.c -o void",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}